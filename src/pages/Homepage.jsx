import React from "react";
import { StickyNavbar } from "../layouts/Navbar";
const Homepage = () => {
   return (
      <div>
         <StickyNavbar />
      </div>
   );
};

export default Homepage;
